{
  "description": "Opt-in integration-retro reminder. Disabled by default: the script exits silently unless MAPMAP_RETRO_HOOK=1 is set in your environment. It only ever emits a reminder message — it never sends anything anywhere. See hooks/README.md.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/scripts/retro-reminder.sh"],
            "timeout": 10
          }
        ]
      }
    ]
  }
}
